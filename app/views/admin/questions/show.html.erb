<h3><%= @question.body %></h3>

<h5>Answers:</h5>

<p><%= link_to 'new answer', new_admin_question_answer_path(@question) %></p>

<ol>
  <%= render @question.answers%>
</ol>
<p>
  <%= link_to 'edit', edit_admin_question_path(@question)%> |
  <%= link_to 'all questions', admin_test_path(@question.test)%>
</p>
<div><%= link_to 'delete', admin_question_path,
                            method: :delete,
                            data: { confirm: 'are you sure?' }
                            %> </div>
