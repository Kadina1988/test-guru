<% flash.each do |key, message| %>
  <% if key == 'alert'%>
    <%= content_tag :p, message.html_safe, class: 'alert alert-danger', role: 'alert' %>
  <% elsif key == 'notice' %>
    <%= content_tag :p, message.html_safe, class: 'alert alert-success', role: 'alert' %>
  <% end %>
<% end %>
